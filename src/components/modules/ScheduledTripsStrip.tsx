import React,{useEffect,useMemo,useRef,useState}from'react';
import{CalendarClock,ChevronRight,Clock3,Copy,MapPin,Pencil,Radio,Trash2,UserRound,Zap}from'lucide-react';
import{useApp}from'../../context/AppContext';
import{soundManager}from'../../lib/audio';
import type{Trip}from'../../types';
const formatSchedule=(iso:string)=>new Date(iso).toLocaleString('es-CL',{weekday:'short',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false});
const formatTime=(iso:string)=>new Date(iso).toLocaleTimeString('es-CL',{hour:'2-digit',minute:'2-digit',hour12:false});
const minutesUntil=(iso:string,now:number)=>Math.ceil((new Date(iso).getTime()-now)/60000);
export const ScheduledTripsStrip:React.FC=()=>{
 const{trips,setSelectedTripForDetail,addNotification,soundMuted,cancelTrip,createTrip}=useApp();const[now,setNow]=useState(Date.now());const lastAlertMinuteRef=useRef<Map<string,number>>(new Map());const[busyId,setBusyId]=useState<string|null>(null);const[message,setMessage]=useState('');
 useEffect(()=>{const timer=window.setInterval(()=>setNow(Date.now()),10000);return()=>window.clearInterval(timer);},[]);
 useEffect(()=>{const unlock=()=>{void soundManager.prime();};window.addEventListener('pointerdown',unlock,true);window.addEventListener('keydown',unlock,true);return()=>{window.removeEventListener('pointerdown',unlock,true);window.removeEventListener('keydown',unlock,true);};},[]);
 const scheduled=useMemo(()=>trips.filter(t=>Boolean(t.scheduledFor)&&!['completed','cancelled'].includes(t.status)).sort((a,b)=>new Date(a.scheduledFor!).getTime()-new Date(b.scheduledFor!).getTime()),[trips]);const waitingReservations=useMemo(()=>scheduled.filter(t=>minutesUntil(t.scheduledFor!,now)>20),[scheduled,now]);
 useEffect(()=>{scheduled.forEach(trip=>{if(!trip.scheduledFor)return;const remaining=minutesUntil(trip.scheduledFor,now);const row=document.querySelector<HTMLElement>(`[data-dispatch-trip-id="${trip.id}"]`);if(row){row.hidden=remaining>20;row.dataset.centralgoReservation='true';row.dataset.reservationLabel=`RESERVA · ${formatTime(trip.scheduledFor)}`;}if(remaining<=10&&remaining>=0){const bucket=Math.floor(now/60000);if(lastAlertMinuteRef.current.get(trip.id)!==bucket){lastAlertMinuteRef.current.set(trip.id,bucket);addNotification('RESERVA PRÓXIMA',`${trip.code} · ${trip.clientName} · retiro ${formatTime(trip.scheduledFor)} · faltan ${Math.max(0,remaining)} min`,'trip',trip.id);if(!soundMuted)void soundManager.prime().then(ok=>{if(ok)soundManager.playReservationAlarm();});}}});},[scheduled,now,addNotification,soundMuted]);
 const cancelReservation=async(trip:Trip)=>{if(busyId)return;if(!window.confirm(`¿Cancelar la reserva ${trip.code} de las ${formatTime(trip.scheduledFor!)}?`))return;setBusyId(trip.id);setMessage('');try{await Promise.resolve(cancelTrip(trip.id,'Reserva cancelada por la central'));setMessage('Reserva cancelada.');}catch(e){setMessage(e instanceof Error?e.message:'No se pudo cancelar la reserva.');}finally{setBusyId(null);}};
 const repeatNextWeek=async(trip:Trip)=>{if(!trip.scheduledFor||busyId)return;const next=new Date(trip.scheduledFor);next.setDate(next.getDate()+7);const exists=scheduled.some(item=>item.id!==trip.id&&item.clientId===trip.clientId&&Math.abs(new Date(item.scheduledFor!).getTime()-next.getTime())<120000);if(exists){setMessage('Ya existe una reserva equivalente para la próxima semana.');return;}setBusyId(trip.id);setMessage('');try{await Promise.resolve(createTrip({clientId:trip.clientId,clientName:trip.clientName,clientPhone:trip.clientPhone,origin:trip.origin,destination:trip.destination,vehicleTypeRequested:trip.vehicleTypeRequested,estimatedDistanceKm:trip.estimatedDistanceKm,estimatedDurationMins:trip.estimatedDurationMins,estimatedFare:trip.estimatedFare,isFixedFare:trip.isFixedFare,fixedFareAmount:trip.fixedFareAmount,paymentMethod:trip.paymentMethod,notes:trip.notes,dispatchMode:trip.dispatchMode,scheduledFor:next.toISOString()}));setMessage(`Reserva semanal creada para ${formatSchedule(next.toISOString())}.`);}catch(e){setMessage(e instanceof Error?e.message:'No se pudo repetir la reserva.');}finally{setBusyId(null);}};
 const editReservation=(trip:Trip)=>{setSelectedTripForDetail(trip);window.setTimeout(()=>window.dispatchEvent(new CustomEvent('centralgo:edit-trip',{detail:{tripId:trip.id}})),0);};
 const style=<style>{`[data-centralgo-reservation="true"]{border-color:rgba(14,116,144,.5)!important;background:linear-gradient(90deg,rgba(224,242,254,.96),rgba(240,249,255,.92))!important;box-shadow:inset 4px 0 0 rgba(2,132,199,.9)!important;color:#0f172a!important}[data-centralgo-reservation="true"]::before{content:attr(data-reservation-label);display:inline-flex;margin:0 0 6px 0;padding:4px 9px;border:1px solid rgba(3,105,161,.35);border-radius:999px;background:#0369a1;color:white;font-size:10px;font-weight:900;letter-spacing:.08em}[data-centralgo-reservation="true"] .text-white,[data-centralgo-reservation="true"] .text-zinc-200,[data-centralgo-reservation="true"] .text-zinc-300,[data-centralgo-reservation="true"] .text-zinc-400{color:#0f172a!important}`}</style>;
 if(!waitingReservations.length)return <>{style}{message&&<div className="rounded-lg border border-sky-300 bg-sky-50 px-3 py-2 text-xs font-bold text-sky-900">{message}</div>}</>;
 return <>{style}<section className="overflow-hidden rounded-2xl border border-sky-300 bg-sky-50 shadow-xl shadow-black/10"><div className="flex items-center justify-between gap-3 border-b border-sky-200 px-3 py-2.5"><div className="flex min-w-0 items-center gap-2"><div className="rounded-lg bg-sky-600 p-1.5 text-white"><CalendarClock className="h-4 w-4"/></div><div className="min-w-0"><h2 className="text-xs font-black text-slate-900">Reservas programadas</h2><p className="truncate text-[10px] text-slate-600">Formato 24 h · pasan a planilla 20 min antes · alarma cada minuto desde 10 min antes</p></div></div><span className="rounded-full bg-sky-700 px-2 py-1 text-[10px] font-black text-white">{waitingReservations.length}</span></div>{message&&<div className="border-b border-sky-200 bg-white px-3 py-2 text-[10px] font-bold text-sky-900">{message}</div>}<div className="flex gap-2 overflow-x-auto p-2.5">{waitingReservations.slice(0,12).map(trip=>{const remaining=minutesUntil(trip.scheduledFor!,now);const automatic=trip.dispatchMode==='automatic';return <article key={trip.id} className="min-w-[310px] max-w-[390px] flex-1 rounded-xl border border-sky-300 bg-white p-3 text-left shadow-sm"><button type="button" onClick={()=>setSelectedTripForDetail(trip)} className="w-full text-left"><div className="flex items-start justify-between gap-2"><div className="flex items-center gap-1.5 text-[11px] font-black text-sky-800"><Clock3 className="h-3.5 w-3.5"/>RESERVA · {formatSchedule(trip.scheduledFor!)}</div><ChevronRight className="h-4 w-4 shrink-0 text-sky-500"/></div><div className="mt-2 flex flex-wrap gap-1.5"><span className={`inline-flex items-center gap-1 rounded-full border px-2 py-1 text-[9px] font-black ${automatic?'border-emerald-300 bg-emerald-50 text-emerald-800':'border-cyan-300 bg-cyan-50 text-cyan-800'}`}>{automatic?<Zap className="h-3 w-3"/>:<Radio className="h-3 w-3"/>}{automatic?'Despacho automático':'Despacho manual'}</span><span className="rounded-full border border-sky-300 bg-sky-50 px-2 py-1 text-[9px] font-bold text-sky-900">Pasa a principal en {Math.max(1,remaining-20)} min</span></div><p className="mt-2 flex items-center gap-1.5 truncate text-[11px] font-normal text-slate-800"><UserRound className="h-3.5 w-3.5 shrink-0 text-slate-500"/>{trip.clientName}</p><p className="mt-1 flex items-center gap-1.5 truncate text-[11px] font-black text-slate-950"><MapPin className="h-3.5 w-3.5 shrink-0 text-emerald-600"/>{trip.origin.address}</p></button><div className="mt-3 grid grid-cols-3 gap-2"><button type="button" onClick={()=>editReservation(trip)} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-lg border border-sky-300 bg-sky-100 px-2 text-[10px] font-black text-sky-900"><Pencil className="h-3.5 w-3.5"/>Editar reserva</button><button type="button" disabled={busyId===trip.id} onClick={()=>void repeatNextWeek(trip)} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-lg border border-indigo-300 bg-indigo-50 px-2 text-[10px] font-black text-indigo-800 disabled:opacity-50"><Copy className="h-3.5 w-3.5"/>Próxima semana</button><button type="button" disabled={busyId===trip.id} onClick={()=>void cancelReservation(trip)} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-lg border border-rose-300 bg-rose-50 px-2 text-[10px] font-black text-rose-800 disabled:opacity-50"><Trash2 className="h-3.5 w-3.5"/>Cancelar</button></div></article>})}</div></section></>;
};
