import React from 'react';

/**
 * Las cancelaciones operativas pertenecen a la central.
 * El conductor puede rechazar una oferta antes de aceptarla, pero una vez
 * iniciada/asignada la carrera no se expone un botón de cancelación en su app.
 */
export const DriverTripCancellationControl: React.FC = () => null;
